/*jslint browser: true*/
/*global $*/

// Initialize Tabs
$(document).ready(function(){
  $('ul.tabs').tabs();
});

// Initialize Collapse Button
$('.button-collapse').sideNav({activationWidth: 50});
